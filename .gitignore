node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# PocketBase — runtime data and the downloaded binary are not committed.
# Schema/seed live in pb_migrations/ (committed); pb_data is reproducible from them.
/pb_data
/.bin

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Production secrets live in /etc/casadecuentos/*.env on the server (chmod 600),
# never in the repo. Only the deploy/*.env.example templates are committed.
